[build]
builder = "NIXPACKS"
buildCommand = "cd finance-backend && npm ci && npm run build"

[deploy]
startCommand = "cd finance-backend && npm run start"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
